#include<stdio.h>
#include<conio.h>
      float per(float x);
      void main()
      {
      float a;
      clrscr();
      printf("enter a number A: ");
      scanf("%f",&a);
      printf("\n 30%% of A = %.2f",per(a));
      getch();
      }
      float per(float x)
      {
      return a*30/100
      }
